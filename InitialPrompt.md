I want to create a multiplayer space exploration game with authoritative backend (/server) that generates a star system with realistic sun and planets, tracks time (it is a real time game wieht pause), and calculate realistic orbital mechnics with respect of gravity for all the obejcts in a solar system using Kepler orbits and a lightweithgh client (/client) that only shows the star system using Three.js/WebGL. On the client side have a HUD with selection Home (player home system) System (the cureent system view) and Consteallation (stars and connections between them) on the top left and time play/stop on the top right. When I click on any object in a system center to it and show me details in HUD at the bottom left. Create a TODO file in the root with list of things you done and things to do next. Consider things carefuly, make a modular easy to support system I plan it as a long term project.

Use Node.js with Typescript for the server. Persists the galaxy with SQLite. Use WebSockets for the multiplayer implementation. Implement basic system of authenicating users by storing their UUID in local storage. Ask user for a name and galaxy they want to join.
